# MCP audit-trail provenance pack

This pack is retained as a distinct provenance profile. It does not establish conformance with `srs.mcp.sdk_enforcement.v0.1` and does not exercise DAGR admission/outcome semantics.

The original pack is preserved under `legacy-pack/` with its provenance, deterministic builder, fixtures, expected output, and conformance script.
