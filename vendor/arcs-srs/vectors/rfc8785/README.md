# RFC 8785 compatibility vectors

The sample in this directory is the JSON Canonicalization Scheme example from RFC 8785 section 3.2.2, represented as an input JSON document and its expected canonical UTF-8 form. The vector is used only to establish canonicalizer compatibility. Copyright and status remain with the RFC authors and the IETF Trust under the RFC publication terms.
